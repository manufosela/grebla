# build output
dist/
.output/

# dependencies
node_modules/

# astro
.astro/

# environment (NUNCA commitear credenciales)
.env
.env.local
.env.*.local

# firebase — claves de servicio / Admin SDK (NUNCA commitear)
serviceAccountKey.json
*firebase-adminsdk*.json
*-adminsdk-*.json
*serviceAccount*.json
.firebase/
firebase-debug.log*
firestore-debug.log*

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
# Karajan v4: el CONTRATO del método se trackea (gate, hooks, roles y reglas)
# para que lo herede cualquiera que clone; los veredictos y cachés locales, no.
.karajan/cache/
.karajan/reviews/
.karajan/*.log
.reviews/
coverage/
.scannerwork/
