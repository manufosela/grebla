---
import Base from '../../../layouts/Base.astro';

/**
 * Editor del juego (JG-16): islas (casas) y rutas de rol del Mapa de Carrera,
 * editables desde la web sin seeds. Solo SUPERADMIN: el glue verifica el rol
 * y redirige fuera a cualquier otro.
 */
---

<Base title="GREBLA — Editor del juego" requireAuth={true}>
  <game-editor></game-editor>

  <script>
    import '../../../client/adminGame.js';
  </script>
</Base>
