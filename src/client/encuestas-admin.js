/**
 * Glue del panel de administración de Encuestas (RMR-TSK-0325). El acceso lo
 * impone la página (requireAdmin) y las reglas de Firestore; el componente lee y
 * escribe /surveys directamente.
 */
import '../components/common/tool-nav.js';
import '../components/survey/survey-admin.js';
